from dataParser import settings
import matplotlib.pyplot as plt # type: ignore
import pandas as pd # type: ignore

# INPUT: 
# - An array of dataframes computed in calc.py, be sure to plot them according to settings.
# - The settings taken from settings.json
# OUTPUT: None

def plot(data: list[pd.DataFrame], settings: settings) -> None:
    # Your code here
    return
